# 公文格式化工具使用说明

本项目保留两套正式方案。两套方案使用相同的公文识别和格式规则，区别只在安装方式和操作入口。

| 方案 | 正式目录 | 适用场景 | 操作入口 |
| --- | --- | --- | --- |
| 方案 A | `方案A_ribbonUI插件` | 经常处理当前打开的公文，希望直接点击按钮 | WPS 顶部“公文格式”选项卡 |
| 方案 B | `方案B_控制台` | 不安装 Ribbon 插件，希望用一个控制台文档处理同目录文件或其他已打开文档 | `公文格式控制台.docm` 中“双击：...”动作文字 |

方案 A 适合固定安装后长期使用；方案 B 适合随文件夹携带、选择同目录文件或跨文档调整选区。两套方案共享同一套识别和格式化核心，保留其中任意一套都不会影响另一套。

## 使用前准备

1. 先备份待处理文档。
2. 建议使用 `.docx` 文件测试，确认结果后再处理正式文件。
3. 关闭修订、文档保护等可能阻止格式修改的功能。
4. 表格、文本框、图片等特殊对象不会按普通正文段落处理。
5. 全文格式化后应重点复核多行标题、落款日期、附件说明、附件序号和附件标题。

## 方案 A：Ribbon 插件

### 安装或更新

1. 完全关闭所有 WPS 文字窗口。
2. 在 PowerShell 中进入本项目目录。
3. 执行：

```powershell
powershell -ExecutionPolicy Bypass -File ".\方案A_ribbonUI插件\install_wps_addin.ps1"
```

4. 重新打开 WPS 文字。
5. WPS 顶部应出现“公文格式”选项卡。

脚本更新后需要重新运行安装命令，并完全重启 WPS，否则 WPS 可能继续使用旧缓存。

### 全文格式化

1. 在 WPS 中打开需要处理的公文，并确认它是当前活动文档。
2. 打开“公文格式”选项卡。
3. 点击“全文格式化”。
4. 按提示确认是否继续以及是否清除原有直接格式。
5. 格式化完成后检查标题、正文层级、日期、落款、附件和页码。
6. 确认无误后保存；建议先“另存为”新文件。

方案 A 只处理当前活动文档，不会弹出文件选择框。

### 局部格式调整

1. 在当前公文中选中需要调整的文字或完整段落。
2. 在“公文格式”选项卡点击对应按钮，例如“正文”“一级标题”“附件序号”。
3. 多个段落可以一次选中后统一处理。
4. 更换处理位置后重新选择文字，再点击相应按钮。

“附件1”这类行应使用“附件序号”；附件名称应使用“附件标题”，两者不是同一种格式。

“设置公文页码”不要求先选择文字。执行后，各节启用奇偶页不同：页码为 4 号（14 磅）宋体半角阿拉伯数字，显示为 `— 1 —`；单页码右对齐并右空一字，双页码左对齐并左空一字；一字线上边缘位于版心下边缘下方 7mm。重复执行不会累积页码或一字线。

### 方案 A 常见问题

- 没有出现“公文格式”选项卡：重新运行安装脚本并完全退出、重启 WPS。
- 修改脚本后效果没有变化：WPS 仍在使用已安装副本，需要重新安装并重启。
- 点击局部格式按钮没有反应：先确认已经选中文字，并且当前窗口是 WPS 文字文档。

## 方案 B：独立控制台宏

正式文件位于 `方案B_控制台`：

- `公文格式控制台.docm`：正式运行入口，内嵌 WPS JSA 宏和 23 个可执行复合宏字段。
- `公文格式化控制台WPS宏.js`：内嵌宏的源码主本，用于审查、测试和重新生成控制台。
- `生成公文格式控制台文档.js`：将最新源码重新嵌入 `DOCM` 的生成器。
- `安装与使用说明.md`：方案 B 的详细说明。
- `按钮绑定表.md`：宏函数和序号对照。
- `公文格式控制台.docx`、`原型_*`：旧说明和实验文件，不是正式运行入口。

### 首次使用

1. 先备份待处理公文。
2. 把 `公文格式控制台.docm` 复制到待处理公文所在文件夹。
3. 用 WPS 文字打开 `公文格式控制台.docm`。
4. 如果 WPS 显示宏或 JSA 安全提示，按本机安全策略允许该文档运行宏。
5. 控制台中的动作均显示为“双击：操作名称”，例如“双击：同目录一键格式化”。
6. 将鼠标放在完整动作文字上双击执行，不要双击表格空白处。

正常使用 `DOCM` 时不需要单独导入 `.js` 文件。控制台必须先保存到文件夹，才能扫描它所在的目录。

控制台按钮使用浅色高光边框和深色阴影边框形成凸起效果。第一次单击时，WPS 可能只显示字段被选中的系统反馈；将鼠标放在完整的“双击：操作名称”文字上双击即可执行。

### 同目录一键格式化

1. 双击完整文字“双击：同目录一键格式化”。
2. 如果同文件夹只有一个 `.doc`、`.docx`、`.docm`、`.wps` 或 `.wpt` 目标文件，脚本自动选择。
3. 如果有多个目标，脚本自动打开 WPS 原生文件选择框。
4. 在文件选择框中使用上下方向键移动到目标文件，按 `Enter` 确认；也可以使用鼠标。
5. 仅当当前 WPS 无法使用原生文件选择框时，脚本才列出文件名和序号，要求输入序号。
6. 脚本打开目标并执行全文格式化；完成后目标文档保持为当前活动文档，便于立即检查结果。

控制台自身不会进入候选列表，即使控制台文件被改名，只要它是当前打开的带控制台宏字段文档也会被排除。`~$` 开头的临时文件和不支持的后缀同样不会进入候选列表。

### 分步选择并格式化

1. 双击“双击：选择同目录目标”，只记录目标文件。
2. 双击“双击：打开已选目标”，打开并激活目标。
3. 双击“双击：格式化已选目标”，执行全文格式化。
4. 双击“双击：查看当前目标”，查看当前记录的文档。

### 处理已经手动打开的文档

1. 在 WPS 中打开待处理公文，但不要关闭控制台。
2. 回到控制台，双击“双击：选择已打开文档”。
3. 如果只有一个非控制台文档，脚本直接选中；如果有多个，输入显示的序号。
4. 再双击“双击：格式化已选目标”。

### 局部格式调整

1. 在待处理公文中选中文字或完整段落，保留该文档窗口。
2. 切回 `公文格式控制台.docm`。
3. 双击“双击：大标题”“双击：正文”“双击：一级标题”“双击：附件序号”“双击：附件标题”等完整动作文字。
4. 如果打开了多个公文，脚本会列出文档名称和选区状态；输入序号选择要处理的文档。

脚本会自动读取目标文档窗口中保留的选区，正常情况下不需要先运行“记录选区”。同一文档打开多个窗口时，优先使用存在实际选中文字的窗口。

“附件1”“附件2”应使用“附件序号”；下一行的附件名称应使用“附件标题”。

“双击：公文页码”会对目标文档设置正式公文页码：4 号宋体、`— 1 —`、奇数页右侧空一字、偶数页左侧空一字、一字线上边缘距版心下边缘 7mm。

### 兼容菜单和源码导入

如果当前 WPS 仍不能执行 `DOCM` 中的动作文字，先确认已经关闭旧控制台并重新打开最新生成的 `公文格式控制台.docm`。仍不兼容时，可以把 `公文格式化控制台WPS宏.js` 完整导入 WPS JS 宏项目：

1. 先运行 `ConsoleSyntaxCheck`，确认脚本完整加载。
2. 再运行 `ConsoleRunMenu`，通过序号菜单执行相同功能。

菜单中的“记录当前选区”等入口主要用于旧版或特殊 WPS 环境。正式 JS 使用纯 ASCII、CRLF 和 Unicode 转义；不要批量还原 `\uXXXX`，也不要另存为 ANSI 或 GBK。

### 保存结果

全文和局部按钮会修改打开的目标文档，但不会自动替用户保存。检查结果后手动保存，正式文件建议先“另存为”。

## 目录合并结果

原来的 `脚本` 和 `方案B_控制台` 已合并为一个正式目录：

```text
方案B_控制台
├─ 公文格式化控制台WPS宏.js
├─ 公文格式控制台.docm
├─ 生成公文格式控制台文档.js
├─ 安装与使用说明.md
├─ 按钮绑定表.md
├─ 示例
└─ 历史参考
```

后续修改逻辑时维护 `方案B_控制台\公文格式化控制台WPS宏.js`，再运行：

```powershell
node ".\方案B_控制台\生成公文格式控制台文档.js"
```

不要从旧 `docx`、`原型_*` 或 `历史参考` 导入过期脚本。
